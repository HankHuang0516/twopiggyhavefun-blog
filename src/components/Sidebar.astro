---
// Sidebar Component - Dynamic Category Tree from Post Tags
import { getCollection } from 'astro:content';

const BASE_URL = '/twopiggyhavefun-blog';

// Helper to fix image paths
function fixImagePath(src: string | undefined): string {
    if (!src) return `${BASE_URL}/placeholder.jpg`;
    if (src.startsWith('http')) return src;
    if (src.startsWith('/') && !src.startsWith(BASE_URL)) {
        return `${BASE_URL}${src}`;
    }
    return src;
}

// Get all posts
const allPosts = await getCollection('posts');
allPosts.sort((a, b) => new Date(b.data.date).valueOf() - new Date(a.data.date).valueOf());
const recentPosts = allPosts.slice(0, 5);

// Tag to display name mapping
const tagDisplayNames: Record<string, string> = {
    'taipei': '台北市',
    'newtaipei': '新北市',
    'taoyuan': '桃園',
    'yilan': '宜蘭',
    'hsinchu': '新竹',
    'miaoli': '苗栗',
    'taichung': '台中市',
    'nantou': '南投',
    'changhua': '彰化',
    'chiayi': '嘉義',
    'yunlin': '雲林',
    'tainan': '台南',
    'kaohsiung': '高雄',
    'pingtung': '屏東',
    'hualien': '花蓮',
    'taitung': '台東',
    'food': '美食餐廳',
    'travel': '景點旅遊',
    'life': '親子生活',
    'japan': '日本旅遊',
    'hongkong': '香港澳門',
    'singapore': '新加坡',
};

// Region grouping
const regionTags = ['taipei', 'newtaipei', 'taoyuan', 'yilan', 'hsinchu', 'miaoli', 'taichung', 'nantou', 'changhua', 'chiayi', 'yunlin', 'tainan', 'kaohsiung', 'pingtung', 'hualien', 'taitung'];
const contentTags = ['food', 'travel', 'life'];
const foreignTags = ['japan', 'hongkong', 'singapore'];

// Count posts by tag
const tagCounts = new Map<string, number>();
const postsByTag = new Map<string, any[]>();

allPosts.forEach(post => {
    const tags = post.data.tags || [];
    tags.forEach((tag: string) => {
        if (tag === 'pixnet') return; // Skip pixnet tag
        tagCounts.set(tag, (tagCounts.get(tag) || 0) + 1);
        if (!postsByTag.has(tag)) postsByTag.set(tag, []);
        postsByTag.get(tag)!.push(post);
    });
});

// Build dynamic categories from actual tags
const dynamicCategories = [
    {
        id: 'taiwan',
        name: '台灣好玩好吃趴趴走',
        count: allPosts.filter(p => {
            const tags = p.data.tags || [];
            return tags.some((t: string) => [...regionTags, ...contentTags].includes(t));
        }).length,
        expanded: true,
        children: contentTags
            .filter(tag => tagCounts.get(tag))
            .map(tag => ({
                id: tag,
                name: tagDisplayNames[tag] || tag,
                count: tagCounts.get(tag) || 0,
                posts: postsByTag.get(tag) || []
            }))
    },
    ...regionTags
        .filter(tag => tagCounts.get(tag))
        .map(tag => ({
            id: tag,
            name: tagDisplayNames[tag] || tag,
            count: tagCounts.get(tag) || 0,
            expanded: false,
            children: [],
            posts: postsByTag.get(tag) || []
        }))
];

// Taiwan day trips
const taiwanTrips = [
    { name: '宜蘭一日遊', slug: 'yilan-1day' },
    { name: '宜蘭兩日遊', slug: 'yilan-2day' },
    { name: '基隆一日遊', slug: 'keelung-1day' },
    { name: '台北捷運一日遊', slug: 'taipei-mrt-1day' },
    { name: '九份瑞芳一日遊', slug: 'jiufen-1day' },
    { name: '桃園一日遊', slug: 'taoyuan-1day' },
];

// Stats
const stats = {
    today: Math.floor(Math.random() * 1000) + 500,
    total: 297216779
};
---

<aside class="sidebar w-full lg:w-80 space-y-6">
    <!-- Article Categories -->
    <div class="bg-white border border-gray-200 p-4">
        <h3 class="text-lg font-bold mb-3 text-red-600">文章</h3>
        
        <!-- Expand/Collapse Links -->
        <div class="text-sm mb-3">
            <a href="javascript:void(0)" id="expandAll" class="text-blue-600 hover:underline">展開全部</a>
            <span class="text-gray-400 mx-1">|</span>
            <a href="javascript:void(0)" id="collapseAll" class="text-blue-600 hover:underline">收合全部</a>
        </div>
        
        <!-- Category Tree -->
        <div class="dtree text-sm">
            {dynamicCategories.map((cat) => (
                <div class="dtree-node" data-id={cat.id}>
                    <div class="flex items-center py-1">
                        {cat.children && cat.children.length > 0 ? (
                            <button class="toggle-btn w-5 text-gray-500 hover:text-gray-700 text-left" data-target={`node-${cat.id}`}>
                                <span class="toggle-icon">{cat.expanded ? '▼' : '▶'}</span>
                            </button>
                        ) : cat.posts && cat.posts.length > 0 ? (
                            <button class="toggle-btn w-5 text-gray-500 hover:text-gray-700 text-left" data-target={`posts-${cat.id}`}>
                                <span class="toggle-icon">▶</span>
                            </button>
                        ) : (
                            <span class="w-5 text-gray-400 text-center">├</span>
                        )}
                        <a href={`${BASE_URL}/category/${cat.id}`} class="text-blue-600 hover:underline">
                            {cat.name}
                        </a>
                        <span class="text-gray-400 text-xs ml-1">({cat.count})</span>
                    </div>
                    
                    {/* Subcategories */}
                    {cat.children && cat.children.length > 0 && (
                        <div class={`sub-categories ml-5 border-l border-gray-300 pl-3 ${cat.expanded ? '' : 'hidden'}`} id={`node-${cat.id}`}>
                            {cat.children.map((child: any, idx: number) => (
                                <div class="py-0.5">
                                    <div class="flex items-center">
                                        <button class="toggle-btn w-4 text-gray-500 hover:text-gray-700 text-left text-xs" data-target={`posts-${child.id}`}>
                                            <span class="toggle-icon">▶</span>
                                        </button>
                                        <a href={`${BASE_URL}/category/${child.id}`} class="text-blue-600 hover:underline ml-1">
                                            {child.name}
                                        </a>
                                        <span class="text-gray-400 text-xs ml-1">({child.count})</span>
                                    </div>
                                    {/* Posts under this category */}
                                    <div class="sub-posts ml-4 border-l border-gray-200 pl-2 hidden max-h-40 overflow-y-auto" id={`posts-${child.id}`}>
                                        {child.posts.slice(0, 10).map((post: any) => (
                                            <div class="flex items-start py-0.5">
                                                <span class="w-2 text-gray-300 text-xs flex-shrink-0">·</span>
                                                <a href={`${BASE_URL}/posts/${post.slug}`} class="text-blue-500 hover:underline ml-1 text-xs line-clamp-1">
                                                    {post.data.title}
                                                </a>
                                            </div>
                                        ))}
                                        {child.posts.length > 10 && (
                                            <div class="text-gray-400 text-xs ml-3">...還有 {child.posts.length - 10} 篇</div>
                                        )}
                                    </div>
                                </div>
                            ))}
                        </div>
                    )}
                    
                    {/* Posts directly under region */}
                    {cat.posts && cat.posts.length > 0 && !cat.children?.length && (
                        <div class="sub-posts ml-5 border-l border-gray-200 pl-2 hidden max-h-40 overflow-y-auto" id={`posts-${cat.id}`}>
                            {cat.posts.slice(0, 10).map((post: any) => (
                                <div class="flex items-start py-0.5">
                                    <span class="w-2 text-gray-300 text-xs flex-shrink-0">·</span>
                                    <a href={`${BASE_URL}/posts/${post.slug}`} class="text-blue-500 hover:underline ml-1 text-xs line-clamp-1">
                                        {post.data.title}
                                    </a>
                                </div>
                            ))}
                            {cat.posts.length > 10 && (
                                <div class="text-gray-400 text-xs ml-3">...還有 {cat.posts.length - 10} 篇</div>
                            )}
                        </div>
                    )}
                </div>
            ))}

            <!-- All Posts -->
            <div class="dtree-node mt-2 pt-2 border-t border-gray-200" data-id="all-posts">
                <div class="flex items-center py-1">
                    <button class="toggle-btn w-5 text-gray-500 hover:text-gray-700 text-left" data-target="node-all-posts">
                        <span class="toggle-icon">▶</span>
                    </button>
                    <span class="text-blue-600 font-medium">全部文章</span>
                    <span class="text-gray-400 text-xs ml-1">({allPosts.length})</span>
                </div>
                <div class="sub-categories ml-5 border-l border-gray-300 pl-3 hidden max-h-80 overflow-y-auto" id="node-all-posts">
                    {allPosts.map((post) => (
                        <div class="flex items-start py-0.5">
                            <span class="w-3 text-gray-400 text-xs flex-shrink-0">├</span>
                            <a href={`${BASE_URL}/posts/${post.slug}`} class="text-blue-600 hover:underline ml-1 text-xs line-clamp-1">
                                {post.data.title}
                            </a>
                        </div>
                    ))}
                </div>
            </div>
        </div>
    </div>

    <!-- Taiwan Day Trips -->
    <div class="bg-white border border-gray-200 p-4">
        <h3 class="text-lg font-bold mb-4" style="color: #c9a961;">台灣各地一日遊二日遊</h3>
        <ul class="space-y-2 text-sm">
            {taiwanTrips.map((trip) => (
                <li>
                    <a href={`${BASE_URL}/tag/${trip.slug}`} class="text-blue-600 hover:underline">
                        {trip.name}
                    </a>
                </li>
            ))}
        </ul>
    </div>

    <!-- Website Stats -->
    <div class="bg-white border border-gray-200 p-4">
        <h3 class="font-bold mb-2 text-red-600">網站人氣統計</h3>
        <div class="text-sm text-gray-600">
            <p>今日人氣：<span class="font-medium">{stats.today.toLocaleString()}</span></p>
            <p>累計人氣：<span class="font-medium">{stats.total.toLocaleString()}</span></p>
        </div>
    </div>

    <!-- Recent Posts -->
    <div class="bg-white border border-gray-200 p-4">
        <h3 class="text-lg font-bold mb-4 text-red-600">近期文章</h3>
        <ul class="space-y-3">
            {recentPosts.map((post) => (
                <li>
                    <a href={`${BASE_URL}/posts/${post.slug}`} class="flex gap-3 group">
                        <img src={fixImagePath(post.data.cover)} 
                             alt={post.data.title}
                             class="w-16 h-16 object-cover flex-shrink-0" />
                        <div class="flex-1 min-w-0">
                            <p class="text-sm text-gray-800 group-hover:text-blue-600 line-clamp-2 transition">
                                {post.data.title}
                            </p>
                            <span class="text-xs text-gray-400">
                                {new Date(post.data.date).toLocaleDateString('zh-TW')}
                            </span>
                        </div>
                    </a>
                </li>
            ))}
        </ul>
    </div>

    <!-- Search -->
    <div class="bg-white border border-gray-200 p-4">
        <form action={`${BASE_URL}/search`} method="get" class="flex">
            <input type="text" name="q" placeholder="搜尋文章..." 
                   class="flex-1 border border-gray-300 px-3 py-2 text-sm focus:outline-none focus:border-blue-500" />
            <button type="submit" class="bg-gray-800 text-white px-4 py-2 text-sm hover:bg-gray-700 transition">
                搜尋
            </button>
        </form>
    </div>
</aside>

<style>
    .dtree-node .sub-categories.hidden,
    .dtree-node .sub-posts.hidden {
        display: none;
    }
    .toggle-btn {
        cursor: pointer;
        background: none;
        border: none;
        padding: 0;
        font-size: 10px;
    }
    .toggle-icon {
        display: inline-block;
        transition: transform 0.2s;
    }
    .line-clamp-1 {
        display: -webkit-box;
        -webkit-line-clamp: 1;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    .line-clamp-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
</style>

<script>
    // Toggle category expansion
    document.querySelectorAll('.toggle-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            e.preventDefault();
            const targetId = btn.getAttribute('data-target');
            if (!targetId) return;
            
            const target = document.getElementById(targetId);
            const icon = btn.querySelector('.toggle-icon');
            
            if (target) {
                const isHidden = target.classList.contains('hidden');
                target.classList.toggle('hidden');
                if (icon) {
                    icon.textContent = isHidden ? '▼' : '▶';
                }
            }
        });
    });

    // Expand All
    document.getElementById('expandAll')?.addEventListener('click', () => {
        document.querySelectorAll('.sub-categories, .sub-posts').forEach(el => el.classList.remove('hidden'));
        document.querySelectorAll('.toggle-icon').forEach(el => el.textContent = '▼');
    });

    // Collapse All
    document.getElementById('collapseAll')?.addEventListener('click', () => {
        document.querySelectorAll('.sub-categories, .sub-posts').forEach(el => el.classList.add('hidden'));
        document.querySelectorAll('.toggle-icon').forEach(el => el.textContent = '▶');
    });
</script>

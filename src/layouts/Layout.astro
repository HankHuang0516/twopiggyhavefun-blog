---
// Layout.astro - Bobowin Style
interface Props {
    title: string;
    description?: string;
}

const { title, description } = Astro.props;
const navLinks = [
    { name: '首頁', href: '/twopiggyhavefun-blog/' },
    { name: '美食', href: '/twopiggyhavefun-blog/category/food' },
    { name: '旅遊', href: '/twopiggyhavefun-blog/category/travel' },
    { name: '生活', href: '/twopiggyhavefun-blog/category/life' },
    { name: '關於我們', href: '/twopiggyhavefun-blog/about' },
];
---

<!doctype html>
<html lang="zh-TW">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content={description || "兩隻小豬的好吃好玩日誌 - 分享美食、旅遊、生活與親子育兒的點點滴滴"} />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title} | 兩隻小豬</title>
        
        <!-- SEO & Open Graph -->
        <meta property="og:title" content={`${title} | 兩隻小豬`} />
        <meta property="og:description" content="兩隻小豬的好吃好玩日誌 - 分享美食、旅遊、生活與親子育兒的點點滴滴" />
        <meta property="og:type" content="website" />
        <meta property="og:site_name" content="兩隻小豬" />

        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;700&display=swap" rel="stylesheet">
    </head>
    <body class="bg-white text-gray-800">
        <!-- Header Layer 1: Site Title -->
        <header class="bg-white border-b border-gray-200">
            <div class="container mx-auto px-4 py-6 text-center">
                <a href="/twopiggyhavefun-blog/" class="inline-block">
                    <h1 class="text-3xl md:text-4xl font-bold text-gray-900 tracking-wide">
                        🐷 兩隻小豬旅行親子生活
                    </h1>
                    <p class="text-gray-500 text-sm mt-1">我們的鏡頭帶著你四處漫遊</p>
                </a>
            </div>
        </header>

        <!-- Header Layer 2: Navigation Bar -->
        <nav class="bg-black sticky top-0 z-50">
            <div class="container mx-auto px-4">
                <ul class="flex flex-wrap justify-center gap-1 md:gap-0">
                    {navLinks.map((link) => (
                        <li>
                            <a href={link.href} 
                               class="block px-4 py-3 text-white text-sm font-medium hover:bg-gray-800 transition">
                                {link.name}
                            </a>
                        </li>
                    ))}
                </ul>
            </div>
        </nav>

        <main class="min-h-screen">
            <slot />
        </main>
        
        <!-- Footer -->
        <footer class="bg-gray-900 text-gray-400 py-8 mt-12">
            <div class="container mx-auto px-4 text-center">
                <p class="mb-2">© 2026 兩隻小豬旅行親子生活. All rights reserved.</p>
                <div class="flex justify-center gap-4 text-sm">
                    <a href="/twopiggyhavefun-blog/about" class="hover:text-white transition">關於我們</a>
                    <a href="mailto:twopiggyhavefun@gmail.com" class="hover:text-white transition">聯絡我們</a>
                </div>
            </div>
        </footer>
    </body>
</html>

<style is:global>
    :root {
        --color-primary: #337ab7;
        --color-black: #000000;
        --color-white: #ffffff;
        --color-gray-light: #f0f0f0;
        --color-text: #333333;
    }
    body {
        font-family: 'Noto Sans TC', 'Microsoft JhengHei', sans-serif;
    }
    a {
        color: var(--color-primary);
    }
    a:hover {
        text-decoration: underline;
    }
    /* Reset link styles for nav and buttons */
    nav a, .post-card a, .sidebar a, button {
        text-decoration: none;
    }
</style>
